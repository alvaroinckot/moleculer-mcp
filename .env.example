# Docker Environment Configuration

# Port configuration
# The port on the host machine
HOST_PORT=3000

# The port inside the container (should match the PORT environment variable)
CONTAINER_PORT=3000

# Optional: Path to a custom settings file inside the container
# Leave empty to use the default CLI mode
SETTINGS_FILE=

# Examples:
# SETTINGS_FILE=/app/settings.json
# SETTINGS_FILE=/app/my-custom-settings.json

# For production, you might want:
# HOST_PORT=80
# CONTAINER_PORT=3000
# SETTINGS_FILE=/app/production-settings.json
